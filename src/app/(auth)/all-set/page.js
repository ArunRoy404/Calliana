import AllSetCard from "@/app/(auth)/all-set/_components/AllSetCard";

export const metadata = {
  title: "You're all set · Calliana",
  description: "Your Calliana password is changed and ready to go.",
};

/** You're all set page — Figma 43:9206. */
export default function AllSetPage() {
  return <AllSetCard />;
}
